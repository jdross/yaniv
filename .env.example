# Database URL. On Render, set DATABASE_URL in the service Environment tab.
# Local example (no password):
DATABASE_URL=postgresql://jdross@localhost/yaniv
# With user/password (e.g. Render Postgres):
# DATABASE_URL=postgresql://user:password@hostname/dbname
